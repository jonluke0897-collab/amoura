# Product Roadmap — Amoura

> Generated by PLAID. Checkboxes are updated as tasks are completed.
> The coding agent MUST mark tasks `- [x]` as they are finished.

**Status:** 56/100 tasks complete
**Current Phase:** Phase 4 — Likes, Matches & Messaging (code-complete, pending live test)

---

## Build Philosophy

1. **Each phase ships something usable.** No phase leaves the app broken. After any phase, you can demo what you've built on a real device.
2. **Infrastructure before features.** Phase 0 sets up Expo, Convex, Clerk, design tokens, and navigation so feature work in later phases is fast and clean.
3. **Magic moment first.** The core value proposition — "scroll feels like home," "conversations start from a real place," "this person actually sees me" — works by the end of Phase 4. Everything else builds on top.
4. **Test as you go.** Don't save testing for the end. Each task includes verification steps you can run on an iOS simulator, an Android emulator, or a physical device via Expo Go / a dev build.
5. **Progressive enhancement.** Start with the simplest working version of each feature, then layer on polish.
6. **Review before proceeding.** Push each completed phase as a PR and let a review agent (e.g. [CodeRabbit](https://coderabbit.ai)) review it before starting the next phase. External review catches issues the coding agent won't flag.
7. **Trans advisors are a release gate.** No phase that touches user-facing copy, identity flows, onboarding, profile, or moderation ships to beta without sign-off from at least one paid trans woman advisor. This is a product principle, not a suggestion.
8. **Architectural safety, not policy safety.** When building any feature that touches how users interact with each other (messaging, likes, profile exposure), prefer designs where the bad behavior is structurally impossible over designs that depend on enforcement. Cf. FR-014 (like-with-comment) and FR-015 (T4T toggle).

---

## Phase 0: Foundation & Setup

> **Goal:** A running Expo app with Convex + Clerk wired up, design tokens configured, a working navigation shell, and a dev build installable on a physical device.

**Reference sections — read these before starting this phase:**
- PRD: § 2 Technical Architecture, § 9 Design System (Tailwind Config), § 10 Auth Implementation, § 13 Dependencies
- Vision: § 5 Design Direction (all subsections — design tokens, typography, spacing, accessibility)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 0. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-0/foundation-and-setup`, commit, push, and open a PR for review."

- [x] **TASK-001** — Initialize Expo app with TypeScript template
  Files: `package.json`, `app.json`, `tsconfig.json`, `.gitignore`, `README.md`
  Notes: Run `npx create-expo-app@latest amoura --template blank-typescript`. Set `app.json` with name "Amoura", slug "amoura", scheme "amoura", bundleIdentifier "com.amoura.app", package "com.amoura.app". Verify: `npx expo start` launches the dev server without errors.

- [x] **TASK-002** — Install and configure Expo Router with file-based routing
  Files: `app/_layout.tsx`, `app/index.tsx`, `app.json`, `package.json`, `babel.config.js`
  Notes: `npx expo install expo-router react-native-safe-area-context react-native-screens expo-linking expo-constants expo-status-bar`. Set `main` in package.json to `expo-router/entry`. Add router plugin in app.json. Create root `app/_layout.tsx` with a `<Stack />` and a placeholder `app/index.tsx`. Verify: app renders the index screen on simulator.

- [x] **TASK-003** — Install and configure NativeWind 4 with Tailwind
  Files: `tailwind.config.ts`, `global.css`, `babel.config.js`, `metro.config.js`, `nativewind-env.d.ts`, `app/_layout.tsx`
  Notes: `npx expo install nativewind tailwindcss react-native-reanimated react-native-safe-area-context`. Follow NativeWind 4 setup: add `jsxImportSource: "nativewind"` in babel. Import `./global.css` in root layout. Verify: a `<View className="bg-plum-500" />` renders with the correct color on a test screen.

- [x] **TASK-004** — Configure Tailwind with the full Amoura design token system
  Files: `tailwind.config.ts`, `global.css`
  Notes: Copy the complete color palette (plum, rose, peach, sage, cream scales), font families (Fraunces, Inter, JetBrainsMono), and spacing scale from docs/prd.md § 9 Design System. Extend theme, don't override defaults. Verify: `bg-plum-500`, `text-rose-400`, `font-heading`, `p-4` all compile and render correctly.

- [x] **TASK-005** — Install and configure Expo Font with Fraunces, Inter, JetBrains Mono
  Files: `app/_layout.tsx`, `assets/fonts/` (downloaded .ttf files)
  Notes: `npx expo install expo-font`. Download Fraunces Variable, Inter Variable, JetBrainsMono Variable from Google Fonts. Use `useFonts` hook in root layout with a `<SplashScreen />` guard until fonts load. Verify: headings render in Fraunces, body in Inter on a test screen.

- [x] **TASK-006** — Set up environment variable handling with expo-constants
  Files: `.env`, `.env.example`, `app.config.ts`, `src/lib/env.ts`
  Notes: Migrate `app.json` → `app.config.ts` so it can read process.env. Add `extra` block exposing EXPO_PUBLIC_CONVEX_URL, EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY, EXPO_PUBLIC_REVENUECAT_IOS_KEY, EXPO_PUBLIC_REVENUECAT_ANDROID_KEY, EXPO_PUBLIC_POSTHOG_KEY. Create typed `env.ts` reader. Add `.env` to `.gitignore`. Verify: `console.log(env.CONVEX_URL)` prints from any component.

- [x] **TASK-007** — Initialize Convex project and scaffold schema
  Files: `convex/schema.ts`, `convex/_generated/` (auto), `convex.json`, `package.json`
  Notes: `npx convex dev` — create a new project, log in, set deployment name to `amoura-dev`. Paste the full schema from docs/prd.md § 3 Data Model into `convex/schema.ts`: users, profiles, photos, prompts, profilePrompts, likes, matches, messages, reports, verifications, subscriptions, blocks, moderationFlags, rateLimitBuckets. Verify: `npx convex dev` deploys without type errors and the schema is visible in the Convex dashboard.

- [x] **TASK-008** — Seed the prompts table with initial prompt set
  Files: `convex/prompts.ts`, `convex/seed.ts`, `scripts/seedPrompts.ts`
  Notes: Create an internal mutation `seedPrompts` that inserts the canonical prompt list (30–40 prompts covering identity, humor, values, desire, community — drafted with trans advisor review). Include `category` and `active` fields per schema. Run once via `npx convex run seed:seedPrompts`. Verify: query `prompts.listActive` returns the full list.

- [x] **TASK-009** — Install and configure Clerk with Expo
  Files: `src/providers/ClerkProvider.tsx`, `app/_layout.tsx`, `.env`, `package.json`
  Notes: `npx expo install @clerk/clerk-expo expo-secure-store expo-web-browser`. Create Clerk account, new app "Amoura", enable Apple and Google OAuth plus email magic links. Copy publishable key to `.env`. Wrap root with `<ClerkProvider publishableKey={...} tokenCache={secureStoreTokenCache}>`. Verify: `useAuth()` hook returns `{ isSignedIn: false }` on cold start.

- [x] **TASK-010** — Wire Convex + Clerk integration with JWT template
  Files: `convex/auth.config.ts`, `src/providers/ConvexProvider.tsx`, `app/_layout.tsx`
  Notes: In Clerk dashboard, create a JWT template named "convex" with the issuer URL. Add `convex/auth.config.ts` per docs/prd.md § 10. Use `ConvexProviderWithClerk` from `convex/react-clerk`. Nest under ClerkProvider in root layout. Verify: after signing in with a test account, `ctx.auth.getUserIdentity()` inside a Convex query returns a non-null identity with the expected subject.

- [x] **TASK-011** — Create Clerk webhook handler to sync users into Convex
  Files: `convex/http.ts`, `convex/users.ts`
  Notes: Implement `httpAction` at `/clerk-webhook` that verifies the Svix signature using CLERK_WEBHOOK_SECRET (set via `npx convex env set`), then handles `user.created`, `user.updated`, `user.deleted` by calling internal mutations to upsert/delete rows in the `users` table keyed by `clerkId`. Register the webhook URL in Clerk dashboard. Verify: creating a test Clerk user triggers a row to appear in Convex `users` table.

- [x] **TASK-012** — Build base navigation shell with auth-gated tabs
  Files: `app/_layout.tsx`, `app/(auth)/_layout.tsx`, `app/(auth)/sign-in.tsx`, `app/(tabs)/_layout.tsx`, `app/(tabs)/browse.tsx`, `app/(tabs)/likes.tsx`, `app/(tabs)/matches.tsx`, `app/(tabs)/profile.tsx`
  Notes: Expo Router groups: `(auth)` for signed-out, `(tabs)` for signed-in. Root layout redirects based on `isSignedIn`. Tab bar has four tabs (Browse, Likes, Matches, Profile) with lucide-react-native icons at 24px. Placeholder screens for each. Verify: signed-out users land on sign-in; signed-in users see tabs; tab switching works.

- [x] **TASK-013** — Set up reusable design-system primitive components
  Files: `src/components/ui/Button.tsx`, `src/components/ui/Input.tsx`, `src/components/ui/Text.tsx`, `src/components/ui/Card.tsx`, `src/components/ui/ScreenContainer.tsx`
  Notes: Build these atoms with NativeWind classes pulling from the Amoura design tokens. Button has variants: primary (plum), secondary (outline), ghost, danger. Text has variants: heading (Fraunces), body (Inter), caption, mono. ScreenContainer applies safe-area and default padding (px-5 py-4). Verify: A demo screen using all five renders correctly with correct typography, colors, and spacing.

- [x] **TASK-014** — Configure EAS Build for dev, preview, and production
  Files: `eas.json`, `app.config.ts`, `.easignore`
  Notes: `npm install -g eas-cli`, `eas login`, `eas build:configure`. Define three profiles: development (internal, dev client), preview (internal distribution), production (store). Set bundleIdentifier and package. Verify: `eas build --profile development --platform ios` produces an installable dev build.

- [x] **TASK-015** — Install PostHog for analytics and wire a test event
  Files: `src/providers/AnalyticsProvider.tsx`, `src/lib/analytics.ts`, `app/_layout.tsx`, `.env`
  Notes: `npm install posthog-react-native`. Create PostHog project, copy API key to `.env`. Wrap providers with `<PostHogProvider client={posthog}>`. Expose `track(event, props)` helper. Fire `app_opened` on cold start. Verify: event appears in PostHog live view within 30s.

- [x] **TASK-016** — Smoke test: push to GitHub, open Phase 0 PR, run review agent
  Files: `.github/` (if PR templates desired), branch `phase-0/foundation-and-setup`
  Notes: `git init`, add remote to a new GitHub repo, commit all work, push. Open a PR titled "Phase 0: Foundation & Setup" with goal + completed tasks in the body. Install [CodeRabbit](https://coderabbit.ai) on the repo. Address any critical feedback before merging. Verify: CodeRabbit posts a review; PR merges cleanly to main.

---

## Phase 1: Identity, Onboarding & Respect Pledge

> **Goal:** A new user can sign up, complete the identity-first onboarding flow (with the extended respect pledge for cis users), and land on a completed (but empty) profile skeleton.

**Reference sections — read these before starting this phase:**
- PRD: § 3 Data Model (users, profiles), § 4 API Specification (users, profiles), § 5 User Stories (Onboarding epic), § 6 Functional Requirements (FR-001 through FR-008), § 8 UI/UX Requirements (Sign In, Identity, Intentions, Respect Pledge standard + extended)
- Vision: § 4 Brand Strategy (voice/tone, anti-patterns), § 5 Design Direction (Design Tokens)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 1. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-1/identity-and-onboarding`, commit, push, and open a PR for review."

- [x] **TASK-017** — Build the Sign In screen with Clerk OAuth + email magic link
  Files: `app/(auth)/sign-in.tsx`, `src/features/auth/SignInCard.tsx`, `src/features/auth/useOAuthFlow.ts`
  Notes: Three buttons: "Continue with Apple", "Continue with Google", "Continue with email". Use `useOAuth` from Clerk for Apple/Google. Email opens a sheet, sends magic link via `signIn.create({ strategy: "email_link", identifier })`. Copy: "You're welcome here." as header. Verify: Apple OAuth completes on iOS simulator and navigates to onboarding.

- [x] **TASK-018** — Build the onboarding gate: route new users through the flow
  Files: `app/(onboarding)/_layout.tsx`, `app/_layout.tsx`, `src/features/onboarding/useOnboardingRoute.ts`, `convex/profiles.ts`
  Notes: After sign-in, check `profiles.getMineStatus()` → returns `{ step: "identity" | "photos" | "prompts" | "intentions" | "pledge" | "complete" }`. Root layout directs to `(onboarding)/[step]` if not complete, else to `(tabs)`. Create layout with a progress indicator (dots or bar) across top. Verify: new users land on `/onboarding/identity`, returning users skip to their next incomplete step.

- [x] **TASK-019** — Build the Identity step (gender identity + T4T toggle for applicable users)
  Files: `app/(onboarding)/identity.tsx`, `src/features/onboarding/IdentityPicker.tsx`, `convex/profiles.ts`
  Notes: Per FR-001/FR-002 and § 8 Identity screen. Options (radio-style chips): "Trans woman", "Trans man", "Non-binary", "Cis woman", "Cis man", "Other", "Prefer not to say". If user picks a trans option, show secondary toggle: "Interested in T4T primarily" (stored as `profile.t4tPreference`). Clear, warm copy: "How do you identify? This shapes who you'll see and who sees you." Save via `profiles.upsertIdentity` mutation. Verify: selecting and continuing persists to Convex, and the next load skips this step.

- [x] **TASK-020** — Build the Intentions step (what are you here for?)
  Files: `app/(onboarding)/intentions.tsx`, `src/features/onboarding/IntentionPicker.tsx`, `convex/profiles.ts`
  Notes: Per FR-003. Multi-select chips: "Long-term partnership", "Short-term dating", "Friendship", "Community", "Figuring it out". Allow up to 3. Save to `profile.intentions` (array of strings). Verify: selections persist and the chip state is restored on back navigation.

- [ ] **TASK-021** — Build the Respect Pledge step (two variants based on identity)
  Files: `app/(onboarding)/pledge.tsx`, `src/features/onboarding/RespectPledgeStandard.tsx`, `src/features/onboarding/RespectPledgeExtended.tsx`, `convex/profiles.ts`
  Notes: Per FR-004/FR-005 and § 8 Respect Pledge. Standard pledge (shown to trans users) is 3 short commitments with one checkbox each. Extended pledge (shown to cis users) is 6 commitments, requires initialing each one (typing initials into an input per line), and includes a 15-second minimum read timer before the Continue button enables. Copy must be reviewed by a trans advisor before this task is marked complete. Save `pledgeAcceptedAt`, `pledgeVersion`, `pledgeType` to profile. Verify: cis user cannot continue without all 6 initials + elapsed timer; trans user sees the shorter version.

- [x] **TASK-022** — Create the Onboarding Complete transition screen
  Files: `app/(onboarding)/complete.tsx`, `src/features/onboarding/WelcomeAnimation.tsx`
  Notes: Warm, celebratory screen with a subtle reanimated fade-and-rise. Copy: "You're in. Let's build your profile — take your time." CTA: "Add photos". Routes to the photos/prompts flow in Phase 2 (placeholder for now). Verify: flow feels continuous; no jarring nav transitions.

- [x] **TASK-023** — Implement profiles.upsertIdentity, upsertIntentions, acceptPledge, getMineStatus
  Files: `convex/profiles.ts`, `convex/schema.ts` (if missing fields)
  Notes: Each mutation validates `ctx.auth.getUserIdentity()`, looks up user by clerkId, and upserts the profile row. `getMineStatus` returns the next incomplete onboarding step based on which fields are populated. Per § 4 API Specification. Verify: Convex dashboard shows rows populated correctly after the onboarding flow.

- [x] **TASK-024** — Add onboarding analytics events
  Files: `src/lib/analytics.ts`, onboarding screens
  Notes: Fire `onboarding_step_completed` with `{ step }` after each step. Fire `onboarding_completed` after pledge. Fire `onboarding_abandoned` on app background during onboarding (via AppState listener). Verify: PostHog funnel shows the four-step sequence.

- [ ] **TASK-025** — Copy review gate with trans advisor
  Files: (no code — coordination task)
  Notes: Send the onboarding copy (identity options, pledge text, intentions copy, welcome text) to the trans advisor(s) retained per product-vision.md. Incorporate feedback. Do not mark complete until at least one advisor has signed off in writing. Verify: written sign-off in a shared doc or email thread. Note the advisor's name and date.

- [x] **TASK-026** — Phase 1 review PR
  Files: branch `phase-1/identity-and-onboarding`
  Notes: Commit and push; open PR "Phase 1: Identity & Onboarding". Include a short Loom or screen recording of the full flow. Let CodeRabbit review. Merge when clean. Verify: PR merged; main branch has working onboarding flow end-to-end.

---

## Phase 2: Profile Creation (Photos + Prompts)

> **Goal:** A signed-in user can complete their profile by uploading photos and answering prompts, and can view their completed profile exactly as others will see it.

**Reference sections — read these before starting this phase:**
- PRD: § 3 Data Model (photos, prompts, profilePrompts), § 4 API Specification (photos, prompts), § 5 User Stories (Browse epic — profile structure only), § 6 Functional Requirements (FR-009 through FR-013), § 8 UI/UX Requirements (Photos, Prompts, Profile)
- Vision: § 5 Design Direction (Design Tokens, spacing, typography)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 2. Then read only the Reference sections listed above. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-2/profile-creation`, commit, push, and open a PR for review."

- [x] **TASK-027** — Install expo-image-picker and expo-image, request permissions
  Files: `src/features/photos/usePhotoPicker.ts`, `app.config.ts`, `package.json`
  Notes: `npx expo install expo-image-picker expo-image expo-image-manipulator`. Add iOS `NSPhotoLibraryUsageDescription` and `NSCameraUsageDescription` with warm copy ("We need access to your photos so you can share your best self"). Hook returns `{ pickFromLibrary, pickFromCamera, cropped }`. Verify: library picker opens on device; permission prompt text matches the warm copy.

- [x] **TASK-028** — Build Convex file storage upload flow with signed URLs
  Files: `convex/photos.ts`, `convex/schema.ts`
  Notes: Implement `photos.generateUploadUrl` (returns Convex storage upload URL), `photos.finalizeUpload` (records storageId + width/height + order in photos table), `photos.remove`, `photos.reorder`. Per § 4 API Specification. Validate ctx.auth. Set max 6 photos per user in `finalizeUpload`. Verify: a photo uploaded from the app appears in Convex storage and is linked in the photos table.

- [x] **TASK-029** — Build the Photos onboarding/edit screen with drag-to-reorder grid
  Files: `app/(onboarding)/photos.tsx`, `src/features/photos/PhotoGrid.tsx`, `src/features/photos/PhotoSlot.tsx`
  Notes: Per § 8 Photos screen and FR-009/FR-010. 2-column grid with 6 slots, empty slots show a dashed border and "+" icon. Long-press to reorder using `react-native-draggable-flatlist`. Tapping a filled slot shows a remove option. Minimum 2 photos to continue. Client-side HEIC→JPEG conversion via expo-image-manipulator, max 2048px long edge, quality 0.85. Verify: uploading, reordering, and removing all persist to Convex and survive app restart.

- [x] **TASK-030** — Build the Prompts onboarding/edit screen
  Files: `app/(onboarding)/prompts.tsx`, `src/features/prompts/PromptPicker.tsx`, `src/features/prompts/PromptAnswerEditor.tsx`, `convex/profilePrompts.ts`
  Notes: Per § 8 Prompts screen and FR-011/FR-012. User picks 3 prompts from a scrollable list of all active prompts (show category labels). For each, a full-screen editor with a 250-char limit, live counter, and a "save" CTA. Store answers in `profilePrompts` table with order 0/1/2. Minimum 3 answered to continue. Verify: 3 prompt-answers persist with the correct order.

- [x] **TASK-031** — Build the Profile tab (self-view) showing the completed profile
  Files: `app/(tabs)/profile.tsx`, `src/features/profile/ProfileView.tsx`, `src/features/profile/PhotoCarousel.tsx`, `src/features/profile/PromptCard.tsx`
  Notes: Per § 8 Profile screen. Scrollable view alternating photos and prompt cards (photo-prompt-photo-prompt-photo-prompt). Matches the exact layout others will see — no separate "preview" mode. Edit button in top-right opens an edit sheet with links to photos/prompts/intentions editors. Verify: visual matches the PRD spec; edits from the sheet reflect immediately (Convex reactivity).

- [x] **TASK-032** — Build a standalone Profile Detail view (used when viewing others)
  Files: `src/features/profile/ProfileDetailScreen.tsx`, `app/profile/[userId].tsx`
  Notes: Per § 8 Profile Detail. Identical layout to the self-view Profile tab but read-only, with action buttons at the bottom (placeholder for like-with-comment in Phase 4). Takes `userId` param, fetches via `profiles.getPublic({ userId })`. Show pronouns, age, distance (mocked for now), intentions as chips. Verify: opening any profile route renders the read-only view.

- [x] **TASK-033** — Implement profiles.getPublic query with field-level privacy
  Files: `convex/profiles.ts`
  Notes: Returns only public fields: displayName, age, pronouns, photos (resolved URLs), prompts, intentions, identityLabel (user-chosen), city. Does NOT return: email, phone, legal name, verification status, pledge timestamps, blocked/blocking lists. Per § 4 API Specification. Verify: calling from a signed-in user returns the subset; sensitive fields are absent from the returned object.

- [x] **TASK-034** — Build photo carousel with pinch-zoom and full-screen view
  Files: `src/features/photos/PhotoCarousel.tsx`, `src/features/photos/PhotoFullScreen.tsx`
  Notes: Horizontal swipe between photos in the profile view, pagination dots at bottom. Tap to open full-screen with pinch-zoom using `react-native-reanimated` + `react-native-gesture-handler`. Swipe down to dismiss. Verify: smooth 60fps swipe between 6 photos; full-screen zoom works.

- [x] **TASK-035** — Add photo verification placeholder (hook for Phase 5)
  Files: `src/features/photos/VerificationBadge.tsx`, `convex/verifications.ts`
  Notes: Stub the `verifications` table row creation and a UI badge component ("verified" check icon in plum). Don't wire Rekognition yet (Phase 5). For now, always render unverified. Verify: badge component renders correctly with both states for QA.

- [x] **TASK-036** — Phase 2 review PR
  Files: branch `phase-2/profile-creation`
  Notes: Commit, push, open PR "Phase 2: Profile Creation". Attach screen recording. Let CodeRabbit review. Merge when clean. Verify: after merge, a brand-new test account can complete sign-up → onboarding → photos → prompts and see their profile.

---

## Phase 3: Browse & Discovery

> **Goal:** A user sees a curated, scrollable feed of profiles filtered by their stated preferences (including T4T toggle), and can tap into any profile to see detail. This is the first part of the magic moment.

**Reference sections — read these before starting this phase:**
- PRD: § 3 Data Model (profiles, blocks), § 4 API Specification (profiles.listFeed), § 5 User Stories (Browse epic), § 6 Functional Requirements (FR-014 — client side, FR-015, FR-016, FR-017), § 8 UI/UX Requirements (Browse, Profile Detail)
- Vision: § 3 Product Strategy (primary metric WAU-TNB), § 5 Design Direction
- GTM: § City-gated Beta Launch Strategy (to understand city filter rationale — GTM doc will be written separately, reference it when available)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 3. Then read only the Reference sections listed above. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-3/browse-and-discovery`, commit, push, and open a PR for review."

- [x] **TASK-037** — Build profiles.listFeed query with preference-aware matching
  Files: `convex/profiles.ts`, `convex/schema.ts` (indexes if missing)
  Notes: Per § 4 API Specification. Inputs: pagination cursor, optional filters. Output: an ordered list of profile summaries (id, firstPhotoUrl, displayName, age, pronouns, topPrompt). Filters by: city match, age range overlap with self's preferences, intention overlap, blocks in either direction excluded, self excluded. When self.t4tPreference is true, rank trans users higher. When `seekingT4TOnly` flag is true, exclude cis users. Verify: manual query from Convex dashboard with three seeded profiles returns expected ordering.

- [x] **TASK-038** — Build the Browse tab feed UI
  Files: `app/(tabs)/browse.tsx`, `src/features/browse/BrowseFeed.tsx`, `src/features/browse/ProfileCard.tsx`
  Notes: Per § 8 Browse screen. Vertical scroll of "profile cards" — one profile per screen height, snap-to-interval scroll. Each card shows first photo, name+age+pronouns, top prompt preview, and a scroll affordance ("tap to see more"). Tapping opens Profile Detail. Empty state when no profiles match: warm copy "No one new to show right now. We're growing — check back tomorrow." Verify: scrolling feels natural; empty state renders correctly.

- [x] **TASK-039** — Wire pagination with infinite scroll and pull-to-refresh
  Files: `src/features/browse/BrowseFeed.tsx`, `src/features/browse/useFeedPagination.ts`
  Notes: Use Convex `usePaginatedQuery` with page size 10. Load next page when within 3 cards of the end. Pull-to-refresh calls reset on the paginated query. Show a subtle loader at the bottom when fetching. Verify: scrolling past card 8 triggers a fetch; pull-to-refresh clears and reloads.

- [x] **TASK-040** — Build Profile Detail screen with full content and back nav
  Files: `app/profile/[userId].tsx`, `src/features/profile/ProfileDetailScreen.tsx`
  Notes: Reuse the ProfileView component from Phase 2. Add a sticky bottom bar with placeholder "Like with a comment" button (wired in Phase 4). Back button in top-left. Verify: navigating from feed to detail and back preserves scroll position in the feed.

- [x] **TASK-041** — Add preference filters sheet (age range, distance, intentions, T4T)
  Files: `src/features/browse/FilterSheet.tsx`, `convex/profiles.ts` (preferences mutation)
  Notes: Per FR-016. Bottom sheet accessible from a filter icon in the Browse tab header. Controls: age dual-slider (18–70), distance single slider (5–100km), intentions multi-select, T4T toggle ("Show T4T matches only"), verified-only toggle (cosmetic until Phase 5). Saves to `profile.preferences`. Applying reloads the feed. Verify: adjusting filters changes the feed content within 500ms.

- [x] **TASK-042** — Implement city detection and set user's city on first browse
  Files: `src/features/location/useLocationCity.ts`, `convex/profiles.ts`
  Notes: `npx expo install expo-location`. Request `requestForegroundPermissionsAsync` on first Browse visit with warm copy. Reverse-geocode to city name. Save as `profile.city` (never raw coords). If user denies, prompt to manually select city from a list of supported launch cities (Austin, Brooklyn, Oakland, LA, Chicago — per GTM plan). Verify: granting location sets city; denying shows the manual picker.

- [x] **TASK-043** — Add "You've seen everyone" end-of-feed state
  Files: `src/features/browse/EmptyStates.tsx`
  Notes: When the feed returns an empty next page, show: "You've met everyone in {city} for now. We add new profiles daily." with a CTA "Adjust filters" that opens the filter sheet. This is critical for low-density launch cities. Verify: seeding 3 profiles, scrolling through all, then seeing the empty state.

- [x] **TASK-044** — Add browse analytics events
  Files: `src/lib/analytics.ts`, `src/features/browse/*`
  Notes: Fire `feed_viewed`, `profile_card_tapped`, `profile_detail_viewed`, `filters_opened`, `filters_applied` with relevant props. Fire `feed_session_duration` on tab blur. Verify: PostHog shows these events during a manual session.

- [x] **TASK-045** — Performance pass on feed rendering
  Files: `src/features/browse/ProfileCard.tsx`, `src/features/browse/BrowseFeed.tsx`
  Notes: Use `expo-image` with `contentFit="cover"` and `cachePolicy="memory-disk"`. FlatList with `removeClippedSubviews`, `windowSize={3}`, `maxToRenderPerBatch={2}`. Profile photos served at 1080px from Convex storage (client requests via query param). Verify: scrolling through 20 profiles holds 60fps on an iPhone 12-class device.

- [x] **TASK-046** — Phase 3 review PR
  Files: branch `phase-3/browse-and-discovery`
  Notes: Commit, push, open PR "Phase 3: Browse & Discovery". Attach a screen recording of scrolling the feed with seeded data. Note in the PR body: "Magic moment #1 (scroll feels like home) is testable with seeded data. Requires real trans users in the city for full effect." CodeRabbit review. Verify: merge; the feed is functional on main.

---

## Phase 4: Likes, Matches & Messaging (the Magic Moment)

> **Goal:** A user can "like with a comment" on a specific photo or prompt, the liked user sees it in their Likes Inbox, can like back to match, and the two can chat. This is the core magic moment of the product.

**Reference sections — read these before starting this phase:**
- PRD: § 3 Data Model (likes, matches, messages), § 4 API Specification (likes, matches, messages), § 5 User Stories (Likes & Matches epic, Messaging epic), § 6 Functional Requirements (FR-014, FR-018 through FR-022), § 8 UI/UX Requirements (Like-with-Comment modal, Likes Inbox, Matches, Chat)
- Vision: § 2 User Research (JTBD — "conversations start from a real place"), § 4 Brand Strategy (voice/tone for system messages)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 4. Then read only the Reference sections listed above. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-4/likes-matches-messaging`, commit, push, and open a PR for review."

- [x] **TASK-047** — Build the Like-with-Comment modal (the architectural anti-fetish gate)
  Files: `src/features/likes/LikeWithCommentModal.tsx`, `src/features/profile/ProfileDetailScreen.tsx`, `app/profile/[userId].tsx`
  Notes: Per FR-014 and § 8 Like-with-Comment. To like, user must first tap a specific photo or prompt on the detail screen — that selection is the "target." The modal then asks for a 20-250 char comment referencing that target. Submit is disabled under 20 chars. The target ref is stored on the like. Critical UX: make "liking without comment" structurally impossible — there is no shortcut. Copy above input: "Comment on what caught your eye. Vague openers ('hey beautiful') don't cut it here." Verify: cannot send a like without selecting a target and writing ≥20 chars.

- [x] **TASK-048** — Implement likes.send mutation with rate limiting
  Files: `convex/likes.ts`, `convex/schema.ts` (rateLimitBuckets)
  Notes: Per § 4 API Specification. Validates: user not blocked either way, target exists, comment length 20-250, target ref is valid (photo ID or prompt ID on that profile). Rate-limit: free tier max 8 likes/day (resets at local midnight — store local offset on user), paid unlimited. Returns `{ likeId, matchCreated: boolean }` — if the recipient had already liked us, create the match atomically and return true. Verify: unit-test the flow where A likes B (no match), then B likes A (match created).

- [x] **TASK-049** — Implement matches.create as part of the likes flow
  Files: `convex/matches.ts`, `convex/likes.ts`
  Notes: Matches are created atomically inside `likes.send` when a reciprocal like exists. Matches table stores `userA`, `userB` (sorted by clerkId lexicographically for stable indexing), `createdAt`, `status: "active" | "unmatched" | "archived"`. Index on both participants. On match creation, create an automatic system message in the messages table with body "You matched. Take your time." Verify: reciprocal like produces a match row and a seed system message.

- [x] **TASK-050** — Build the Likes Inbox tab (inbound likes you haven't acted on)
  Files: `app/(tabs)/likes.tsx`, `src/features/likes/LikesInbox.tsx`, `src/features/likes/LikeCard.tsx`, `convex/likes.ts`
  Notes: Per § 8 Likes Inbox and FR-019. Query `likes.listInbound` returns likes sent to me that I haven't responded to. Each card shows the liker's first photo, name, the comment they wrote, and the target (photo or prompt excerpt). Two actions: "Like back" (creates match), "Pass" (soft-delete/hide). Free tier shows blurred photos + "Upgrade to see who likes you" CTA on a paywall. Verify: paid tier sees clear photos; free tier sees blur + paywall.

- [x] **TASK-051** — Build likes.respond mutation (like back or pass)
  Files: `convex/likes.ts`, `convex/matches.ts`
  Notes: `likes.respond({ likeId, action: "match" | "pass" })`. If match: create match + delete the like row (it's now a match). If pass: soft-delete the like (keep for anti-abuse audit, but mark hidden from the recipient). Verify: "Like back" creates a match and removes the like from the inbox.

- [x] **TASK-052** — Build the Matches tab (list of active matches with last-message preview)
  Files: `app/(tabs)/matches.tsx`, `src/features/matches/MatchList.tsx`, `src/features/matches/MatchRow.tsx`, `convex/matches.ts`
  Notes: Per § 8 Matches and FR-020. Query `matches.listMine` returns matches sorted by last-message-at descending. Each row: other user's photo, name, last message excerpt, relative timestamp. Unread indicator (dot) when there are unread messages. Tapping opens chat. Verify: list updates reactively when a new message arrives.

- [x] **TASK-053** — Build the Chat screen (1:1 messaging with reactive updates)
  Files: `app/chat/[matchId].tsx`, `src/features/chat/ChatScreen.tsx`, `src/features/chat/MessageBubble.tsx`, `src/features/chat/ChatInput.tsx`, `convex/messages.ts`
  Notes: Per § 8 Chat screen and FR-021. Use Convex `useQuery(api.messages.listByMatch, { matchId, paginationOpts })`. Keyboard-aware input at bottom with send button. System message at top ("You matched with X. Opening message was their like-comment."). Render user and other bubbles differently. Auto-scroll to bottom on new message. Mark-as-read mutation on screen focus. Verify: two test accounts can send messages in real time with < 1s latency.

- [x] **TASK-054** — Implement messages.send with content moderation placeholder
  Files: `convex/messages.ts`, `convex/moderation.ts`
  Notes: Per FR-022. Validates: match is active, sender is a participant, body is 1-2000 chars. Inserts a message row with `createdAt`, marks the match's `lastMessageAt`. Stub moderation: set a `flagged: false` default. Actual moderation (keyword/ML check) happens in Phase 5. Verify: messages appear in both users' chats reactively.

- [x] **TASK-055** — Set up OneSignal push notifications for new messages and matches
  Files: `src/providers/NotificationProvider.tsx`, `convex/notifications.ts`, `app.config.ts`
  Notes: `npm install onesignal-expo-plugin react-native-onesignal`. Add OneSignal plugin in `app.config.ts`. On login, register device with user's Convex userId as external user ID. Create a Convex scheduled action that fires on message insert and sends a push to the recipient (title: "New message from {name}", body: message preview if paid tier, generic "You have a new message" if free). Also fire on match creation. Permission request is deferred to the first match or message sent — not on app launch. Verify: sending a message to an offline device delivers a push within 10s.

- [x] **TASK-056** — Add typing indicators and read receipts (paid tier)
  Files: `src/features/chat/TypingIndicator.tsx`, `convex/messages.ts` (ephemeral subscription)
  Notes: Use Convex mutations with TTL for "typing" state. Read receipts update a `readAt` field on the latest incoming message when the screen is focused. Free tier: no typing, no read receipts (show a paywall teaser). Verify: paid-to-paid chat shows typing within 500ms; free account sees neither.

- [x] **TASK-057** — Handle match unmatch flow (either party can unmatch)
  Files: `src/features/matches/UnmatchAction.tsx`, `convex/matches.ts`
  Notes: Per FR-020. In Chat header, three-dot menu with "Unmatch" (confirm modal: "This will remove the conversation for both of you. Continue?"). Sets match status to "unmatched", hides from both users' lists, preserves rows for audit. Neither user can re-match without a new like. Verify: unmatching removes the row from both users' match lists.

- [x] **TASK-058** — Add magic-moment analytics
  Files: `src/lib/analytics.ts`, chat/likes features
  Notes: Fire `like_sent`, `like_received`, `match_created`, `first_message_sent` (marks the 2nd magic moment), `conversation_has_5_messages` (3rd magic moment proxy). Build a PostHog funnel: feed_session → like_sent → match_created → first_message_sent. Verify: full funnel is populated from a test session.

- [ ] **TASK-059** — Phase 4 review PR + live test with 3 real users
  Files: branch `phase-4/likes-matches-messaging`
  Notes: Commit, push, open PR. Before merging, install the dev build on 3 real trans-advisor testers' phones and have them use it together. Fix any UX issues flagged. CodeRabbit review. Merge when clean. Verify: trans advisors confirm the like-with-comment mechanic feels right, not friction-for-friction's-sake.

---

## Phase 5: Safety, Verification & Moderation

> **Goal:** Every user is verified (ID + photo), users can report harmful behavior, moderators can review and take action, and the product has credible protection against fetishizers and bad-faith users.

**Reference sections — read these before starting this phase:**
- PRD: § 3 Data Model (reports, verifications, blocks, moderationFlags), § 4 API Specification (reports, verifications, blocks), § 5 User Stories (Reporting & Safety epic), § 6 Functional Requirements (FR-023 through FR-027), § 8 UI/UX Requirements (Report Flow), § 12 Edge Cases (moderation)
- Vision: § 1 Core Values ("Safety is a product feature"), § 3 Product Strategy (risk mitigations)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 5. Then read only the Reference sections listed above. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-5/safety-and-verification`, commit, push, and open a PR for review."

- [ ] **TASK-060** — Integrate Persona for ID verification
  Files: `src/features/verification/IDVerification.tsx`, `convex/verifications.ts`, `convex/http.ts`
  Notes: `npm install @persona/embedded-sdk-react-native`. Create Persona account + Amoura template. On first sign-in post-onboarding, prompt for ID verification (modal, dismissible twice then required on 3rd open — tracked on user). Open Persona flow; on completion, webhook fires to Convex `/persona-webhook` which updates `verifications.idStatus` to "verified" | "failed". Verify: completing ID verify in Persona sandbox updates the Convex row.

- [ ] **TASK-061** — Integrate AWS Rekognition for photo selfie verification
  Files: `src/features/verification/SelfieVerification.tsx`, `convex/verifications.ts`, `convex/http.ts`, AWS Lambda (external)
  Notes: Per FR-024. User takes a live selfie matching a random pose ("turn head left"). Upload to Convex storage, trigger a Convex action that calls an AWS Lambda (in the same region) wrapping Rekognition `CompareFaces` against their most recent profile photo. If similarity > 90% and liveness confirmed → verifications.photoStatus = "verified". Show a plum check badge on verified profiles. Verify: a test account with matching selfie and profile photo gets the badge; mismatch does not.

- [ ] **TASK-062** — Add "verified only" filter to Browse (wiring from Phase 3 cosmetic toggle)
  Files: `src/features/browse/FilterSheet.tsx`, `convex/profiles.ts`
  Notes: Wire the existing verified-only toggle to actually filter the feed. Free tier: filter is a paid feature with paywall on toggle. Paid tier: filter works. Verify: toggling filter with paid account reduces the feed to only verified users.

- [ ] **TASK-063** — Build the Report Flow (step-by-step guided reporter)
  Files: `src/features/reports/ReportSheet.tsx`, `src/features/reports/ReportReasonPicker.tsx`, `app/report/[targetUserId].tsx`, `convex/reports.ts`
  Notes: Per § 8 Report Flow and FR-025. Triggered from any profile's three-dot menu or from chat header. Multi-step: (1) pick reason — Fetishizing behavior, Harassment, Transphobia, Impersonation, Unsafe content, Spam, Other; (2) optional text (1000 chars); (3) optional attach-chat-context (if triggered from chat, auto-attaches last 20 messages); (4) submit. Creates a `reports` row. Confirmation screen: "Thanks. Our team reviews every report within 24 hours." Verify: report submission creates a Convex row with all context.

- [ ] **TASK-064** — Build the blocks system (user-initiated, immediate)
  Files: `src/features/blocks/BlockAction.tsx`, `convex/blocks.ts`, `convex/profiles.ts` (listFeed filter), `convex/likes.ts`, `convex/messages.ts`
  Notes: Per FR-026. Block from profile menu or chat header. Confirm modal. On block: insert `blocks` row (blocker, blocked), remove any existing match, hide from feeds both ways, block new likes both ways, block new messages both ways. Unblock flow lives in Settings → Blocked Users. Verify: after A blocks B, A cannot see B and B cannot see A anywhere in the app.

- [ ] **TASK-065** — Build the internal moderation dashboard (web, Next.js)
  Files: `admin/` directory or separate repo, `convex/moderation.ts`
  Notes: Separate web app (Next.js + Clerk + Convex). Only users with `users.role = "moderator"` can access. Shows open reports, allows actions: dismiss, warn user (send in-app notification), temporarily suspend, permanently ban. All actions logged in a `moderationActions` audit table. Per the PRD spec; treat as a polished internal tool, not consumer-facing. Verify: moderator can see a submitted report and take action; target user's state updates accordingly.

- [ ] **TASK-066** — Add simple automated content moderation on messages
  Files: `convex/moderation.ts`, `convex/messages.ts`
  Notes: On message insert, scan against a keyword list of slurs and obvious fetish language (list curated with advisors — keep in a private file `convex/moderationKeywords.ts` that is gitignored or env-loaded). If matched, set `message.flagged = true` and create a `moderationFlags` row visible to moderators. Do NOT auto-delete messages (causes false positives that harm trans users using reclaimed language). Moderators review and decide. Verify: a test message with a flagged keyword creates a flag row.

- [ ] **TASK-067** — Build the "Safety Center" in Settings
  Files: `app/(tabs)/profile.tsx`, `src/features/settings/SafetyCenter.tsx`
  Notes: Per FR-027. Settings section with: Verify ID, Verify photo, Blocked users, Report history, Safety tips (written with advisor, links to external resources). Plain language, no legalese. Verify: all links navigate correctly; blocked users list is mutable.

- [ ] **TASK-068** — Add crisis-sensitive empty states and error copy
  Files: `src/features/**/*EmptyState.tsx`, `src/features/errors/*`
  Notes: Review every empty state and error message with a trans advisor. Specifically: rejection from ID verification, paywall copy, zero-results Browse, zero-likes Inbox, being unmatched. No shame, no friction-blame, no "you did something wrong" tone. Verify: advisor sign-off on the consolidated copy document.

- [ ] **TASK-069** — Phase 5 review PR
  Files: branch `phase-5/safety-and-verification`
  Notes: Commit, push, open PR. Explicitly include a "Safety review" section in the PR body listing: every moderation path, every report reason, every block surface, every verification path. Require a second human reviewer (not just CodeRabbit) — ideally the trans advisor. Verify: merge only after both automated and human review pass.

---

## Phase 6: Subscription, Paywall & Monetization

> **Goal:** Free users can meaningfully use the app but encounter tasteful paywalls; paid users get unlimited likes, see who liked them, advanced filters, and read receipts. Subscriptions process through RevenueCat on both App Store and Play Store.

**Reference sections — read these before starting this phase:**
- PRD: § 3 Data Model (subscriptions), § 4 API Specification (subscriptions, webhooks), § 5 User Stories (Subscription epic), § 6 Functional Requirements (FR-028, FR-029), § 8 UI/UX Requirements (Paywall), § 11 Payment Integration (full section)
- Vision: § 3 Product Strategy (primary metric, pricing rationale)
- GTM: § Pricing & Monetization (when gtm.md is available)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 6. Then read only the Reference sections listed above. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-6/subscription-and-paywall`, commit, push, and open a PR for review."

- [ ] **TASK-070** — Configure RevenueCat project, products, and entitlements
  Files: (external — RevenueCat dashboard), `.env`
  Notes: Create RevenueCat project. Configure App Store Connect with products `amoura_plus_monthly_1499` ($14.99/mo) and `amoura_plus_yearly_9999` ($99.99/yr). Mirror in Google Play Console. In RevenueCat: one offering "default", one entitlement "plus" that both products grant. Save iOS + Android SDK keys to `.env`. Verify: products appear in RevenueCat dashboard as "approved" for testing.

- [ ] **TASK-071** — Install and wire react-native-purchases SDK
  Files: `src/providers/RevenueCatProvider.tsx`, `src/lib/purchases.ts`, `app/_layout.tsx`
  Notes: `npx expo install react-native-purchases`. Configure on app start with the right key per platform. Identify user with Clerk userId (`Purchases.logIn(clerkUserId)`). Expose hooks: `useEntitlement("plus")`, `usePurchase(productId)`. Verify: `Purchases.getCustomerInfo()` returns a valid customer object on app start.

- [ ] **TASK-072** — Build the Paywall screen
  Files: `app/paywall.tsx`, `src/features/paywall/PaywallScreen.tsx`, `src/features/paywall/PlanCard.tsx`
  Notes: Per § 8 Paywall. Full-screen modal route. Hero: "Love without limits." Feature list with checkmarks: See who likes you, Unlimited likes, Advanced filters, Read receipts, Typing indicators. Two plan cards: monthly ($14.99) and yearly ($99.99, "save 44%" badge). Legal copy: auto-renewal, cancel anytime, link to Terms/Privacy. Restore Purchases button. Close X in top-right. Verify: UI matches spec; purchase flow launches native sheet on button tap.

- [ ] **TASK-073** — Wire RevenueCat webhooks to Convex for server-side entitlement
  Files: `convex/http.ts`, `convex/subscriptions.ts`
  Notes: Per § 11 Payment Integration. Register a webhook URL with RevenueCat (`/revenuecat-webhook`). Verify HMAC signature using RC_WEBHOOK_SECRET. Handle events: INITIAL_PURCHASE, RENEWAL, CANCELLATION, EXPIRATION, BILLING_ISSUE. Upsert `subscriptions` table keyed by userId. Never trust client-side for entitlement checks — always re-read from Convex. Verify: sandbox purchase fires webhook and creates a `subscriptions` row with `isActive: true`.

- [ ] **TASK-074** — Create the `isPlus` selector and gate paid features server-side
  Files: `convex/subscriptions.ts`, `convex/likes.ts`, `convex/profiles.ts`, `convex/messages.ts`
  Notes: Helper `isPlus(ctx, userId)` queries the subscriptions table. Rate-limit `likes.send` to 8/day when `!isPlus`. In `likes.listInbound`, return blurred/redacted versions for free users. In `profiles.listFeed`, disable verified-only filter for free users. Verify: free-tier account hits the daily limit exactly at 8; after upgrade via RC sandbox, limit lifts immediately on next query.

- [ ] **TASK-075** — Place paywall triggers at the right moments
  Files: `src/features/likes/LikesInbox.tsx`, `src/features/browse/FilterSheet.tsx`, `src/features/chat/TypingIndicator.tsx`, `src/features/likes/LikeWithCommentModal.tsx`
  Notes: Per FR-028. Show the paywall when: user opens Likes Inbox (free tier sees blur), user tries to enable verified-only filter, user hits daily like cap (don't block — show the paywall with "8 likes today, come back tomorrow or upgrade"). Never gate safety features (reports, blocks, ID verify). Verify: each trigger opens the paywall and respects the user's close/dismiss behavior.

- [ ] **TASK-076** — Build Manage Subscription screen
  Files: `src/features/settings/ManageSubscription.tsx`, `app/(tabs)/profile.tsx` (link)
  Notes: Per FR-029. Shows current plan, next renewal date, payment method source (App Store / Play Store). CTA: "Manage in App Store" / "Manage in Play Store" (uses Linking to open the native management URL — Apple and Google require this for cancellation). Restore Purchases button. Verify: button opens the correct native management flow on each platform.

- [ ] **TASK-077** — Add subscription analytics events
  Files: `src/lib/analytics.ts`, paywall + subscription flows
  Notes: Fire `paywall_viewed` with `{ trigger }`, `paywall_dismissed`, `checkout_started`, `purchase_completed` with `{ product, price }`, `purchase_restored`, `subscription_canceled` (from webhook). Integrate RevenueCat events with PostHog per RC docs. Verify: end-to-end purchase appears in both RC and PostHog.

- [ ] **TASK-078** — Phase 6 review PR with StoreKit testing
  Files: branch `phase-6/subscription-and-paywall`
  Notes: Commit, push, open PR. Test with an App Store Connect sandbox account on a real device, and a Google Play internal testing track. Validate: purchase works, webhook fires, entitlement unlocks, restore works, cancellation reflects within 1 min. CodeRabbit review. Verify: merge when all three storefronts (Apple sandbox, Google internal, RevenueCat dashboard) agree on the subscription state.

---

## Phase 7: Polish, Accessibility & Launch Prep

> **Goal:** The app feels cohesive and polished across every flow, meets WCAG 2.1 AA, has complete error/empty states, landing page, app store listings, and is ready for beta launch in 3 cities.

**Reference sections — read these before starting this phase:**
- PRD: § 7 Non-Functional Requirements (all), § 12 Edge Cases & Error Handling (full), § 14 Open Questions
- Vision: § 5 Design Direction > Accessibility Commitments, § 4 Brand Strategy (tone across error copy)
- GTM: § Pre-Launch Checklist, § App Store Listing, § Launch Day Plan (reference when gtm.md is available)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 7. Then read only the Reference sections listed above. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-7/polish-and-launch-prep`, commit, push, and open a PR for review."

- [ ] **TASK-079** — Comprehensive empty/error/loading state pass
  Files: `src/features/**/*` (all screens)
  Notes: Every screen must have: a loading skeleton (not just a spinner — use `expo-image` blurhashes where possible), an empty state with warm copy, and an error state with a retry action. Audit each screen against § 12 Edge Cases. Verify: manually disable network, step through every main screen, confirm no raw error dialogs and no infinite spinners.

- [ ] **TASK-080** — Comprehensive form validation pass with Zod schemas
  Files: `src/lib/validation/*.ts`, all form screens
  Notes: Per § 7 NFR validation. Create Zod schemas for: profile edits, prompt answers (20-250 chars), like comments (20-250 chars), message bodies, age ranges, report text. Show inline errors below fields as user types (debounced 300ms). Disable submit until valid. Verify: each form shows the right error message for each failure mode.

- [ ] **TASK-081** — Accessibility pass: labels, roles, contrast, reduced motion
  Files: `src/components/ui/*`, all feature screens
  Notes: Per § 7 NFR accessibility (WCAG 2.1 AA). Every interactive element gets `accessibilityLabel` and `accessibilityRole`. VoiceOver/TalkBack walkthrough of full app. Check all text/background contrast ratios (use a tool like `@accessibility/color-contrast`). Respect `useReduceMotion` — disable entrance animations for users who prefer reduced motion. Dynamic Type support: all text uses scaled font size. Verify: VoiceOver users can complete sign-up → like → match → message with audio only.

- [ ] **TASK-082** — Responsive layout across device sizes
  Files: all screen components
  Notes: Test on: iPhone SE (small), iPhone 15 Pro (standard), iPhone 15 Pro Max (large), iPad mini (tablet), Pixel 7 (medium Android), Galaxy Fold (unfolded). Ensure photo grids, chat, and modals scale without layout breakage. Use `Platform.select` sparingly for platform-specific tweaks. Verify: screenshots on all 6 device sizes look intentional.

- [ ] **TASK-083** — Performance audit and optimization
  Files: cross-cutting
  Notes: Per § 7 NFR performance. Measure: cold start time (target <2.5s), time-to-interactive on feed (<1.5s), frame rate on feed scroll (60fps), chat message render time (<100ms p95). Use `expo-dev-client` + React DevTools Profiler. Optimize expensive re-renders with memoization. Pre-fetch next feed page at card 6. Verify: all four metrics hit targets on an iPhone 12-class device and a mid-tier Android.

- [ ] **TASK-084** — Crash reporting with Sentry
  Files: `src/providers/SentryProvider.tsx`, `app/_layout.tsx`, `.env`
  Notes: `npm install @sentry/react-native`. Create Sentry project. Initialize with DSN in `.env`. Wrap root with Sentry error boundary. Configure source maps upload for EAS builds. Tag events with userId (Clerk) and app version. Verify: a test crash from a dev build appears in Sentry within 1 minute with a readable stack trace.

- [ ] **TASK-085** — Rate limiting and anti-abuse backstop
  Files: `convex/rateLimitBuckets.ts`, `convex/likes.ts`, `convex/messages.ts`, `convex/reports.ts`
  Notes: Per § 7 NFR. Beyond the free-tier daily cap, add absolute caps: max 100 likes/day, max 500 messages/day, max 20 reports/day (per user). Token-bucket implementation on the `rateLimitBuckets` table. Also: IP-based sign-up rate limit (10/day per IP) via Clerk restrictions + Convex webhook. Verify: attempting to exceed limits returns a clear error message.

- [ ] **TASK-086** — Build the marketing landing page (Next.js, separate repo or `/web`)
  Files: `web/` (Next.js app), or separate repo `amoura-web`
  Notes: Simple responsive landing page at amoura.app. Sections: hero with app screenshots + "Join the beta — Austin, Brooklyn, Oakland", 3-feature strip (like-with-comment, T4T toggle, verified by default), founder story (short, honest: "Built with, not for"), beta signup form (email + city dropdown), FAQ, footer with privacy/terms. Submit stores email in a Convex `waitlist` table. Verify: page loads < 1s; signup writes to Convex.

- [ ] **TASK-087** — App store assets: screenshots, icon, privacy labels, listing copy
  Files: `assets/store/` (screenshots), `app.config.ts` (icon), App Store Connect, Play Console
  Notes: Icon: Amoura logo in plum on cream, 1024x1024 with proper padding. 6 screenshots per platform showing: feed, profile, like-with-comment, match moment, chat, profile. Listing description written per § 8.4 GTM Strategy (reference once gtm.md exists). Apple Privacy Labels: Identify user, contact, photos, location (city), usage data — all linked to user. No third-party ad tracking. Verify: store review preview shows all assets correctly on both stores.

- [ ] **TASK-088** — Legal: Privacy Policy, Terms of Service, Community Guidelines
  Files: `web/pages/privacy.tsx`, `web/pages/terms.tsx`, `web/pages/community.tsx`
  Notes: Work with a lawyer experienced in LGBTQ+ platforms (budget line item). Community Guidelines specifically call out: anti-fetishization, respect for pronouns, no outing, no screenshots-for-shame, zero-tolerance for transphobia. Must be understandable at a 10th-grade reading level. Linked from in-app paywall, settings, and onboarding. Verify: lawyer sign-off; links resolve from all required surfaces.

- [ ] **TASK-089** — GDPR/CCPA data export and delete flows
  Files: `src/features/settings/DataExport.tsx`, `convex/userData.ts`, `convex/http.ts`
  Notes: Per § 7 NFR privacy. Settings → "Your Data" has: Download my data (emails a JSON bundle within 72h, via Convex scheduled action), Delete my account (confirm modal, 14-day grace period, then hard-deletes all user data including Convex storage photos). Verify: export email arrives; delete removes all traces after grace period.

- [ ] **TASK-090** — Customer support channel setup
  Files: `src/features/settings/Support.tsx`, external (Front, Help Scout, or similar)
  Notes: In-app "Contact support" opens email to support@amoura.app (set up a shared inbox — Front/Help Scout/basic Gmail to start). SLA: 24h for general, 4h for safety issues. Include a "Safety issue" category that routes to a higher-priority queue. Verify: sending a test support request from the app arrives at the inbox.

- [ ] **TASK-091** — Pre-launch QA checklist: full regression on all flows
  Files: `docs/qa-checklist.md`, `docs/test-scripts/`
  Notes: Write a QA doc with every user flow end-to-end, check it against 2 real devices (iOS + Android) with 2 sandbox accounts. Fix any regression. Note bugs that are known but punted to post-launch. Have at least one trans advisor complete a full flow as a final check. Verify: advisor + QA sign-off in the doc.

- [ ] **TASK-092** — Phase 7 review PR and TestFlight/Play Internal release
  Files: branch `phase-7/polish-and-launch-prep`, EAS + store submissions
  Notes: Commit, push, open PR. Run `eas submit --profile production --platform ios` and `eas submit --profile production --platform android`. Submit to TestFlight and Play Internal Testing. Distribute to 30 invited beta users across the 3 launch cities. Merge when clean. Verify: 30 beta invites delivered; first 5 installs complete.

---

## Phase 8: Post-Launch Iteration (Evolving)

> **Goal:** Respond to real-user signal from beta: fix critical bugs, refine the match-quality algorithm, ship high-leverage P2 features, prepare for public launch expansion.

**Priorities in this phase will shift based on real user feedback. The tasks below are starting placeholders, not a fixed plan.**

**Reference sections — read these before starting this phase:**
- PRD: § 14 Open Questions (now informed by real usage), § 6 Functional Requirements (P2 items only)
- GTM: § Post-Launch Growth (when available)
- PostHog dashboards (external)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 8. Then read only the Reference sections listed above plus the current week's user-feedback summary document (I'll paste it into the chat). Focus on the first 3 unchecked tasks."

- [ ] **TASK-093** — Instrument in-app NPS / prompt-for-feedback after 7 days
  Files: `src/features/feedback/NPSPrompt.tsx`, `convex/feedback.ts`
  Notes: 7 days post-onboarding, show a one-question sheet: "How likely are you to recommend Amoura to a friend?" 0-10 slider + optional text. Store in `feedback` table. Run weekly NPS calc. Verify: prompt fires at the right time for test accounts created 7 days ago.

- [ ] **TASK-094** — Add "Suggest a prompt" surface for community-driven prompt growth
  Files: `src/features/settings/SuggestPrompt.tsx`, `convex/prompts.ts`
  Notes: Settings → "Suggest a prompt". Text input, 200 char limit, category picker. Creates a `promptSuggestions` row. Moderators review weekly, best ones get added to the active prompt pool. Verify: a submitted suggestion appears in moderator queue.

- [ ] **TASK-095** — Build an internal metrics dashboard (WAU-TNB, funnel, retention)
  Files: `admin/metrics/` (Next.js), or Metabase/Retool connected to Convex
  Notes: Key charts: WAU-TNB by cohort, onboarding funnel conversion, feed session → like → match → message funnel, D1/D7/D30 retention, paid conversion rate, report rate by reason. Verify: dashboard loads and reflects yesterday's data.

- [ ] **TASK-096** — Iterate on match-quality algorithm based on behavioral data
  Files: `convex/profiles.ts`, `convex/ranking.ts`
  Notes: Extract feed ranking into a dedicated `ranking.ts`. Add signals: mutual intentions overlap (+), T4T preference both ways (+), geographic proximity decay, report-count decay (−), verification (+), recent activity (+). A/B test variants via PostHog feature flags. Verify: lift in like rate on treatment vs control > 10% over a 1-week window.

- [ ] **TASK-097** — Placeholder — user-driven feature from top 3 feedback themes
  Files: TBD
  Notes: Aggregate feedback from NPS + support emails + community Discord. Pick the highest-leverage P2 request. Scope as 5-10 tasks. Verify: decision rationale documented in `docs/decisions/`.

- [ ] **TASK-098** — Placeholder — second user-driven feature
  Files: TBD
  Notes: Same process.

- [ ] **TASK-099** — Expand to cities 4-6 (post-beta)
  Files: `convex/cities.ts`, marketing site, GTM coordination
  Notes: Per GTM plan. Gate on density metric: when launch city has > 500 active trans users, open the next tier. Verify: new city gets its own Discord community thread, ambassadors identified, marketing launch activated.

- [ ] **TASK-100** — Scalability review at 5k users
  Files: cross-cutting
  Notes: At 5k+ MAU, audit Convex query perf, storage costs, RevenueCat volume, OneSignal delivery rate, Clerk MAU pricing tier. Right-size or refactor anything approaching a threshold. Verify: documented costs projection through 50k MAU.

---

## Beyond Phase 8

Further work — web client (expand TAM), community events, expansion internationally, potential features like video prompts, voice notes, scheduled-date prompts — is intentionally left unplanned. PLAID documents are a living foundation, not a permanent script. When you finish Phase 7 and have 3 months of real user data, regenerate this roadmap's future phases informed by what you've learned.

---

## Agent Session Guide

### How to Use This Roadmap with Your Coding Agent

1. **Start a session:** Give your coding agent the phase prompt at the beginning of each phase.
2. **Read selectively:** Each phase lists its Reference sections — the specific parts of the PRD, vision doc, and GTM doc needed for that phase. The agent should read only those sections, not the entire documents.
3. **Let it work:** The agent reads the roadmap, finds the first unchecked task, implements it, and marks it complete.
4. **One session = one phase (ideally):** Try to complete a full phase in one session for best continuity. If you need to stop, the agent can resume from the last unchecked task.
5. **Push a PR for review:** When a phase is complete, push the work as a PR and let a review agent (e.g. [CodeRabbit](https://coderabbit.ai)) review it before starting the next phase. For Phases 1, 5, and 7, also get a human trans advisor reviewer — non-negotiable.
6. **Need more context?** If a task references a section not in the phase's Reference sections, the agent should read just that section on demand.

### Session Tips

- **Don't read everything:** The PRD, vision doc, and GTM doc can be large. Each phase's Reference sections tell the agent exactly what to read. Loading the full documents wastes context.
- **Don't skip tasks:** Tasks are ordered intentionally. Skipping creates dependency issues.
- **Verify after each phase:** Run the app on a real device after completing a phase to confirm everything works before moving on.
- **Review before moving on:** Push a PR for each completed phase and let your review agent check it. Don't start the next phase until the PR is merged. This catches issues early when they're cheap to fix.
- **Update the status line:** After completing tasks, update the header status: `**Status:** X/Y tasks complete` and `**Current Phase:** Phase N`.
- **Advisor gate on sensitive phases:** Phases 1, 5, and 7 include user-facing copy and safety systems. Do not ship these without written trans advisor sign-off.

### Prompt Templates

**Starting a new phase:**
> "Read docs/product-roadmap.md and find the current phase. Read only the Reference sections listed for that phase from docs/prd.md, docs/product-vision.md, and docs/gtm.md. Start working on the first unchecked task. After completing each task, update the checkbox to [x] in the roadmap file. Continue through the phase."

**Resuming after a break:**
> "Read docs/product-roadmap.md. Find where we left off (first unchecked task). Read only the Reference sections listed for the current phase from docs/prd.md, docs/product-vision.md, and docs/gtm.md. Continue from the first unchecked task."

**After completing a phase:**
> "Phase [N] is complete. Create a branch called phase-{N}/{slug}, commit all work, push, and open a PR targeting main. Title it 'Phase {N}: {Title}' and include the phase goal and completed task count in the body."

**Fixing an issue:**
> "There's a problem with [description]. Read the relevant section of docs/prd.md for the expected behavior and fix it. Don't mark any new tasks complete until the fix is verified."

**Requesting advisor review:**
> "This phase includes user-facing copy. Generate a consolidated copy document at docs/copy-reviews/phase-{N}.md pulling every user-facing string from files I've changed this phase. I'll send it to the advisor."
